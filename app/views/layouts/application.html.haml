!!!
%html
  %head
    %title Merjis
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    - if current_user.present?
      #user_info Logged in as #{current_user.email} | #{link_to 'Logout', sign_out_path}

    %p.notice= notice
    %p.alert= alert

    = yield
